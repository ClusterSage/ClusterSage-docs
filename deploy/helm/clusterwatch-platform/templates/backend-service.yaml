apiVersion: v1
kind: Service
metadata:
  name: clusterwatch-backend
  namespace: {{ .Release.Namespace }}
spec:
  selector: { app: clusterwatch-backend }
  ports:
    - name: http
      port: 80
      targetPort: http
