apiVersion: v1
kind: Secret
metadata:
  name: clusterwatch-secrets
  namespace: {{ .Release.Namespace }}
type: Opaque
stringData:
{{- range $k, $v := .Values.backend.secrets }}
  {{ $k }}: {{ $v | quote }}
{{- end }}
