# Frontend Context

Frontend path: `apps/frontend`. It uses Next.js App Router, TypeScript, Tailwind CSS, a small API wrapper in `src/lib/api.ts`, protected dashboard shell, auth forms, cluster views, issues/log pages, settings, agent key generation, and dynamic Helm install guide. The frontend stores the user JWT in localStorage for this scaffold.
